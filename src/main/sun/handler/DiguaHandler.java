package sun.handler;

import org.springframework.web.servlet.HandlerExecutionChain;

public class DiguaHandler extends HandlerExecutionChain {
    public DiguaHandler(Object handler) {
        super(handler);
    }
}
