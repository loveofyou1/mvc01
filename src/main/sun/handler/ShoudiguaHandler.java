package sun.handler;

import org.springframework.web.servlet.HandlerExecutionChain;

public class ShoudiguaHandler extends HandlerExecutionChain {
    public ShoudiguaHandler(Object handler) {
        super(handler);
    }
}
